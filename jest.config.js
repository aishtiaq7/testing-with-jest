module.exports = {
  testEnvironment: "node", // Specifies the testing environment, e.g., Node.js or jsdom for browser-like environments.
  testMatch: ["**/*.test.js"], // Matches test files with '.test.js' suffix.
  verbose: true, // Provides detailed test results in the output.
  collectCoverage: true, // Enables code coverage collection to track how much of your code is tested.
};
